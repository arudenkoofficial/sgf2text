name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest

    strategy:
      fail-fast: false
      matrix:
        # Developed on the newest Node; 24 is the LTS the package promises to
        # support in `engines`. Running both keeps that promise tested rather
        # than asserted.
        node: ['24', '26']

    steps:
      - uses: actions/checkout@v7

      - uses: actions/setup-node@v7
        with:
          node-version: ${{ matrix.node }}
          cache: npm

      - run: npm ci

      # Tests run against the TypeScript sources directly, so a construct Node
      # cannot strip fails here rather than at someone's runtime.
      - run: npm test

      - run: npm run typecheck

      - run: npm run typecheck:web

      - run: npm run build

      - run: npm run build:web
